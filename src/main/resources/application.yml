spring:
  application:
    name: escala-igreja

  datasource:
    url: jdbc:postgresql://localhost:5433/igreja
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
